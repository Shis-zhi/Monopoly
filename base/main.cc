#include"main.h"
#include<iostream>
using namespace monopoly;
int main(){
    std::vector<Player> player(4);
    return 0;

    while(1){
        {//遍历四个玩家
            //状态修改 state
            //自由操作（查询资产，卖地至多一次，道具次数？）
            //掷骰子、行进（判定路障、炸弹）
            //行进结果判定
        }
    }
}